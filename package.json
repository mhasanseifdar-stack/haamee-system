{
  "name": "haamee-system",
  "version": "1.0.0",
  "description": "Haamee Management System",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm install && node server.js"
  },
  "engines": {
    "node": "18.x"
  }
}
