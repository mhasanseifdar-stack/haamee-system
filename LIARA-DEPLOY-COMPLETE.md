# 🚀 راهنمای Deploy کامل روی لیارا

---

## 📦 فایل‌های لازم:

1. ✅ `liara.json` (آماده شده)
2. ✅ `backend/server.js` (آپدیت شده)
3. ✅ `frontend/src/App.js` (آپدیت شده)

---

## 🔧 مرحله 1: آپلود فایل‌ها به GitHub (5 دقیقه)

### 1.1 کپی فایل‌های جدید

**فایل 1: liara.json**
- کپی کن فایل `liara.json` 
- بذارش در **ریشه پروژه** (کنار پوشه‌های backend و frontend)

```
haamee-system/
├── liara.json          ← اینجا!
├── backend/
└── frontend/
```

**فایل 2: server.js**
- فایل جدید `server.js` رو
- جایگزین کن با `backend/server.js` قدیمی

**فایل 3: App.js**
- فایل جدید `App.js` رو  
- جایگزین کن با `frontend/src/App.js` قدیمی

---

### 1.2 Push به GitHub

باز کن **Git Bash** در پوشه اصلی:

```bash
git add .
git commit -m "Ready for Liara deployment"
git push origin main
```

---

## 🎯 مرحله 2: Deploy در لیارا (5 دقیقه)

### 2.1 در پنل لیارا:

1. ✅ برنامه `panel-haamee` که ساختی رو باز کن
2. ✅ برو به تب **"استقرار"**
3. ✅ کلیک **"استقرار جدید"**
4. ✅ صبر کن 5-7 دقیقه

---

### 2.2 مراحل Deploy:

لیارا خودکار این کارها رو انجام میده:

```
1. ✅ Frontend رو Build می‌کنه (npm run build)
2. ✅ Backend رو نصب می‌کنه (npm install)
3. ✅ سرور رو اجرا می‌کنه (node server.js)
4. ✅ Frontend از Backend Serve میشه
```

---

### 2.3 گرفتن URL:

بعد از Deploy موفق، یه URL میگیری:

```
https://panel-haamee.liara.run
```

---

## 🌐 مرحله 3: اتصال دامنه (اختیاری - 5 دقیقه)

### 3.1 در لیارا:

1. تب **"دامنه‌ها"**
2. **"افزودن دامنه"**
3. وارد کن: `panel.hamee.org`
4. یه CNAME بهت میده

### 3.2 در DirectAdmin:

1. DNS Management
2. Add CNAME:
   - Name: `panel`
   - Points to: `panel-haamee.liara.run`
   - TTL: 3600
3. Save

---

## ✅ تست نهایی:

1. برو به URL: `https://panel-haamee.liara.run`
2. Login: `admin` / `admin123`
3. تست همه چیز!

---

## 🔧 اگه خطا داد:

### خطا: "Build failed"
- چک کن `liara.json` در ریشه پروژه باشه
- چک کن توی GitHub push شده باشه

### خطا: "Cannot GET /"
- صبر کن 2 دقیقه
- Refresh کن

### خطا: "API error"
- چک کن `App.js` آپدیت شده باشه
- باید `API_URL = '/api'` باشه (نه localhost)

---

## 📞 پشتیبانی لیارا:

اگه مشکل داشتی:
- تلگرام: @liara_ir
- ایمیل: support@liara.ir

---

## 🎉 تمام!

**پروژه‌ت الان آماده‌ست!** 🚀
